// WAP to find minimum amount of coins to accumulate an amount
#include <iostream>
#include <vector>
using namespace std;

int findMinCoins(int amount, int coinsUsed) {
    // Base Case
    if(amount == 0)
        return;
    if()
    // Recursive Case

}

int main() {
    vector<int> coins = {1, 2, 5};
    int amount = 11;
    int coinsUsed = 0;
    int sum = 0;

    cout << amount - findMinCoins(amount, coinsUsed) << endl;

    return -1;
}